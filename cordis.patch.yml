# agent-tui bundle: the executable client startup. It does not replace or patch
# the official WebUI; it only adds the standalone terminal surface when the
# client-only profile explicitly includes this bundle.
- insert:
    - id: agent-tui-startup
      name: 'agent-tui/plugin-startup'
