/** Compatibility face; Markdown semantics are owned by text-parser-plugin. */
export {
  IncrementalMarkdownTokenizer,
  tokenizeAssistantMarkdown,
  type MarkdownMode,
  type MarkdownSemanticToken,
} from '../../text-parser-plugin/src/text-parser-plugin.ts'
